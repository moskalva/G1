{
    "version": "2.0.0",
    "configurations": [
        {
            "name": ".NET Core Attach",
            "type": "coreclr",
            "request": "attach"
        },

        {
            "name": "Debug Server",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/G1.Server/G1.Server.csproj"
        }
        
    ]
}